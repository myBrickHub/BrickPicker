<resources>
    <!-- Base application theme. -->
    <style name="Theme.BrickPicker" parent="Theme.MaterialComponents.DayNight.DarkActionBar">
        <!-- Primary brand color. -->
        <item name="colorPrimary">@color/lightOrange</item>
        <item name="colorPrimaryVariant">@color/brightLightOrange</item>
        <item name="colorOnPrimary">@color/white</item>
        <!-- Secondary brand color. -->
        <item name="colorSecondary">@color/lightBlue</item>
        <item name="colorSecondaryVariant">@color/blue</item>
        <item name="colorOnSecondary">@color/black</item>
        <!-- Status bar color. -->
        <item name="android:statusBarColor">?attr/colorPrimaryVariant</item>
        <!-- Customize your theme here. -->
        <item name="android:textColorSecondary">@color/textDarkSecondary</item>
    </style>

    <style name="Theme.BrickPicker.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>

    <style name="Divider">
        <item name="android:layout_width">match_parent</item>
        <item name="android:layout_height">1dp</item>
        <item name="android:background">?attr/colorPrimary</item>
    </style>

    <style name="BackgroundPrimary">
        <item name="android:background">@color/lightOrange</item>
    </style>

    <style name="NavBar">
        <item name="android:background">@drawable/side_nav_bar</item>
    </style>

    <style name="DetailsCard">

    </style>

    <style name="Theme.BrickPicker.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar" />

    <style name="Theme.MyApplication.PopupOverlay" parent="ThemeOverlay.AppCompat.Light" />

</resources>